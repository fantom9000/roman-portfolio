---
import CasePage from "@components/CasePage.astro";
import { projects } from "@data/projects";

const project = projects.find((item) => item.slug === "peptidy");
if (!project) {
  throw new Error("Project peptidy not found");
}
---

<CasePage project={project} />
