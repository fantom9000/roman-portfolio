---
import CasePage from "@components/CasePage.astro";
import { projects } from "@data/projects";

const project = projects.find((item) => item.slug === "quiz");
if (!project) {
  throw new Error("Project quiz not found");
}
---

<CasePage project={project} />
